using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StatsSidebar : MonoBehaviour
{
    public GameObject icyJuiceCount, thunderJuiceCount, cloverJuiceCount, radiateJuiceCount, kingJuiceCount, emptyBottleCount, nicotineCount, VGJuiceCount, PGJuiceCount; 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
